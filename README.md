# To run the server application follow below steps
* npm install
* npm build
* npm start

# application runs at http://localhost:8000

# API are hosted at heroku: https://evening-tor-29203.herokuapp.com/api/v1